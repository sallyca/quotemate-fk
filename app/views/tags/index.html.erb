<h1>Наведём порядок?</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tags.each do |tag| %>
  <tr>
    <td><%= link_to "# #{tag.keyword}", quotes_path(:tag => tag.id) %></td>
    <td><%= link_to 'Trash', tag, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tag', new_tag_path, {:class=>"submit"} %>
